h1 "Listing accounts"

table do
  tr do
    th { 'Name' }
    th { 'Industry' }
  end

  for account in @accounts
    tr do
      td { account.name }
      td { account.industry }
      td { link_to 'Show', account }
      td { link_to 'Edit', edit_account_path(account) }
      td { link_to 'Destroy', account, :confirm => 'Are you sure?', :method => :delete }
    end
  end
end

br

link_to 'New account', new_account_path
