<h1>Listing sources</h1>

<table>
  <tr>
    <th>Name</th>
    <th>URL</th>
  </tr>

<% for source in @sources %>
  <tr>
    <td><%=link_to source.name,edit_source_path(source) %></td>
    <td><%=source.url%></td>
    <td><%= link_to 'X', source, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />
<%= link_to "Load all sources from YAML", :id=>"1",:action=>"pick_load"  %><br/>
<%= link_to "Save all sources to YAML", :id=>"1",:action=>"pick_save"  %><br/>
<%= link_to 'Create new source', new_source_path %><br/>
